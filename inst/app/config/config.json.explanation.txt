{
    "Username": "USER", // default username
    "Language": "en",  // defualt language for requests
    "Locale": "nl-NL", // default locale used by the headless browser
    "ExportFolder": "./ImportExport/", // export and inport folder for configurations (csv) and data (csv)
    "port": 9033, // default port used by user interface
    "Browser": "c:/Program Files/Mozilla Firefox/firefox.exe", // default browser to start for user interface
    "Headless": "firefox", // default headless browser to use for scraping
    "proxy": "", // set a proxy address if you your headless browser needs to go to Internet via a proxy
    "database": "observationDB.sqlite", // default sqlite database to use
    "OneObservationPerDay": false,  // If true the tool stores only the last observation in the database. Any new observation at the same day overwrites the previous.
    "proxy_auth": "" // ????
}